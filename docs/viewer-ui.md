# Viewer UI (adiado)

A documentação da `viewer-ui` está temporariamente adiada — o código será refatorado.

Esta página serve como placeholder. Após a refatoração, documentaremos a API TypeScript e os assets do viewer.
